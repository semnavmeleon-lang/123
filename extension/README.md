# VSK OSAGO — пакетная проверка пролонгации

Chrome-расширение (Manifest V3): читает номера полисов из Excel-файла,
прогоняет каждый через форму проверки пролонгации на `lka.vsk.ru`,
и записывает результат в единственную выбранную колонку — но не в сам
исходный файл, а в отдельную его копию, которую расширение создаёт
специально для этого. Исходный файл не открывается на запись и не
меняется ни байтом. Никаких прямых HTTP-запросов к бэкенду ВСК — всё
идёт через настоящую страницу в вашем браузере с вашей авторизацией,
расширение просто нажимает то же самое, что нажимали бы вы руками.

## Установка

1. `chrome://extensions` → включить «Режим разработчика» (переключатель
   в правом верхнем углу).
2. «Загрузить распакованное расширение» → выбрать папку `extension/`.
3. Открыть панель: клик по иконке расширения на панели инструментов
   Chrome (иконка по умолчанию — свой значок не задавали).

## Использование

Панель управления открывается в отдельной вкладке и должна оставаться
открытой всё время прогона — в ней хранится состояние и дескриптор
файла.

1. **Файл.** «Открыть Excel-файл…» — выберите исходный `.xlsx`. Он
   открывается только на чтение — прав на запись для него не
   запрашивается и он никак не меняется.
2. **Колонки.** Выберите колонку с номерами полисов и колонку для
   результата (должны уже существовать в файле — добавьте пустую
   колонку в Excel заранее, если нужно). Выбор запоминается по
   названию заголовка для следующих запусков. В работу берутся только
   строки, где ячейка результата ещё пустая, ничем не закрашена и не
   содержит комментария — остальные считаются уже обработанными и
   пропускаются. «Ничем не закрашена» — это про реальный, заметный
   глазу цвет: белая заливка и заливка цветом темы по умолчанию (то,
   что часто стоит на всей колонке после «Форматировать как таблицу»
   или в шаблонах) в счёт не идут, иначе на многих файлах в работу не
   попадало бы вообще ничего. Если после запуска написано, что в
   колонке нет номеров полисов, но они есть — журнал теперь отдельно
   покажет, сколько строк с номером было найдено и отброшено именно
   фильтром, чтобы было видно, в чём причина.

   При первом клике «Начать проверку» расширение попросит выбрать, **куда
   сохранить результат** — по умолчанию предложит имя вида «‹исходный
   файл› — результаты.xlsx» рядом с исходным. Этот новый файл сразу
   создаётся как полная копия исходного (те же данные, форматирование,
   комментарии), и дальше в него точечно дописывается только колонка
   результата, по одной ячейке за раз. В пределах одной сессии панели
   этот вопрос задаётся только один раз — «Стоп» и повторное «Начать
   проверку» продолжают писать в тот же файл результата.

   Отсюда же — продолжение прерванного прогона: остановили проверку на
   60-м договоре, закрыли панель, а позже открыли **файл результата**
   (не исходный) как источник для новой проверки — расширение видит,
   что первые 60 строк результата уже не пустые (цена, ошибка или
   «Пропущено вручную» — не важно), пропускает их и начинает ровно с
   61-й; при первом «Начать проверку» в этот раз можно выбрать сохранять
   поверх того же файла результата или создать ещё один. Если строку
   нужно перепроверить заново — просто очистите её ячейку результата
   перед открытием файла.
3. **Элементы страницы.** «Открыть страницу ВСК в рабочей вкладке»
   создаёт/открывает рабочую вкладку на `lka.vsk.ru/products/forge/osago`
   — это единственное место, где расширение само переходит по адресу.
   Дальше в этой вкладке всё делаете вручную: вводите тестовый номер
   полиса, жмёте «Найти», ждёте, пока данные подтянутся и т. д. Семь
   независимых элементов ниже, у каждого своя кнопка «Назначить» и своя
   «Сбросить», можно переназначить один элемент, не трогая остальные —
   и «Назначить» берёт элемент именно с той страницы, которая прямо
   сейчас открыта, а не перезагружает её заново. Поэтому, например,
   активную «Продолжить» можно назначить, только когда она реально
   стала активной на экране.
   - чекбокс «Предыдущий полис ВСК» — у него отдельная галочка
     «Пропускать этот шаг»: если включить, расширение не будет ни
     искать, ни отмечать этот чекбокс (и назначать ему селектор не
     нужно) — полезно, когда для части полисов «предыдущего полиса
     ВСК» нет и отмечать нечего;
   - поле ввода номера полиса;
   - кнопка «Найти» (нажимается сразу после ввода номера);
   - кнопка «Продолжить», **пока она ещё неактивна** — кликается сразу
     после «Найти», до загрузки данных полиса;
   - кнопка «Продолжить», **когда она уже активна** — кликается после
     того, как данные подтянулись;
   - варианты результата «Цена» и «Невозможно рассчитать» — их можно
     назначить вручную (когда нужный экран уже открыт в рабочей
     вкладке), либо не трогать и один раз показать «на лету» во время
     обычного прогона (см. ниже).

   При каждом из двух кликов расширение запоминает не только селектор,
   но и снимок состояния кликнутого элемента (`disabled`, классы,
   `aria-disabled`). На проверке оно ждёт, пока кнопка «Продолжить»
   реально перейдёт в состояние, снятое при клике «активная» — а не
   просто появится в DOM. Это работает одинаково что при разных
   селекторах у двух состояний, что при полностью одинаковом (частый
   случай — это одна и та же кнопка на странице всё время, меняется
   только её класс/`disabled`): раз оба клика попали на один элемент,
   панель напишет об этом в журнал информационно, без необходимости
   что-то переназначать — проверка всё равно будет ждать именно смены
   состояния, а не факта существования кнопки.

   Клики во время назначения форму не отправляют — только запоминают
   элемент.
4. **Запуск.** «Начать проверку» — расширение само проходит по всем
   отобранным строкам: открывает страницу, ждёт (не по таймеру, а по
   реальному появлению элементов) чекбокс/поле/кнопку «Найти», отмечает
   чекбокс, вводит номер, жмёт «Найти», дожидается появления активной
   «Продолжить», жмёт её, затем ждёт результат. Если по строке за
   разумное время нет вообще никакого ответа (вкладка зависла или ушла
   на непредвиденную страницу) — строка помечается ошибкой в журнале и
   в файле, и работа продолжается со следующей. «Пропустить текущий
   договор» рядом со «Стоп» делает то же самое вручную, сразу: если
   видно, что с конкретным договором что-то не так и ждать таймаут не
   хочется — жмёте её, в файл пишется «Пропущено вручную», и проверка
   сразу переходит к следующему.

### Первое распознавание результата

Систему может вернуть один из двух исходов — цену или сообщение
о невозможности расчёта. За один прогон виден только один из них,
поэтому оба селектора расширение просит показать «на лету»: как
только на странице появляется исход, для которого ещё нет
сохранённого селектора, прогон ставится на паузу и в панели
появляется запрос «кликните на результат», а затем — «это цена или
невозможно рассчитать?». После первого раза по каждому из двух
исходов подсказки больше не нужны, и весь список проходит уже без
остановок.

Если ВСК позже поменяет вёрстку страницы и какой-то селектор перестанет
попадать — «Сбросить» у конкретного элемента сбрасывает обучение только
для него, остальные не трогаются, и его можно назначить заново без
переустановки расширения.

## Как записывается результат

Исходный файл открывается только на чтение и не трогается вообще —
права на запись для него не запрашиваются. При первом «Начать
проверку» расширение через системный диалог «Сохранить как» создаёт
**новый** файл (по умолчанию рядом с исходным, с суффиксом
«— результаты»), сразу заполняя его полной копией исходного. Именно в
этот новый файл дальше и пишутся результаты — исходный остаётся ровно
таким, каким был.

Запись в файл результата — не обычный экспорт Excel-библиотеки поверх
всей книги (это способно незаметно изменить форматирование, стили или
потерять комментарии в других ячейках) и не разбор/пересборка XML через
DOM (у этого нашёлся собственный способ незаметно повредить файл — см.
ниже), а точечная правка прямо в тексте XML внутри самого .xlsx (это
zip-архив): нужный тег `<c>` находится точным поиском по самой строке
файла и заменяется, всё остальное содержимое остаётся тем же самым
куском текста, буквально не проходя ни через какой разбор. Архив
пересобирается с этой одной изменённой частью, не трогая `styles.xml`,
комментарии, другие листы и остальные ячейки. Проверено (в том числе
независимыми от JS Python-библиотеками `openpyxl`/`ElementTree`, не
только собственным кодом): после записи в одну ячейку остальные
строки, их заливка и комментарии остаются побайтово идентичны той
копии, с которой файл результата начинался, а сам файл открывается
без ошибок.

Первая версия делала эту правку через `DOMParser`/`XMLSerializer` —
разобрать XML в объект, поменять нужный узел, сериализовать обратно.
На практике это оказалось небезопасно для настоящих файлов: в реальном
Chrome (в отличие от среды, где это тестировалось) `XMLSerializer`
добавляет собственную декларацию `<?xml ... ?>` при сериализации ранее
разобранного документа, и вместе с уже добавленной вручную декларацией
получались две подряд — невалидный XML, который Excel открывал только
через «восстановление повреждённого файла», а часть содержимого при
этом терялась. Отсюда и переход на точечную правку текста: она в
принципе не пересобирает документ через сериализатор, так что такой
целый класс проблем просто не может возникнуть, независимо от того,
что ещё нестандартного есть в конкретном файле.

Запись происходит сразу после каждой строки, а не в конце — так
частично пройденный прогон не теряется, даже если его остановить или
закрыть браузер. Дескриптор файла результата и вся связанная с записью
память живут только в вкладке панели, пока она открыта, поэтому:

- вкладка панели помечается как незакрываемая Chrome'ом при нехватке
  памяти (`autoDiscardable: false`) — иначе неактивная долгое время
  вкладка может быть тихо выгружена и затем незаметно перезагружена
  «с нуля», без файла и без журнала;
- файл результата создаётся через `showSaveFilePicker`, который сам
  выдаёт право на запись как часть выбора файла — отдельно
  запрашивать его не нужно, и запись из асинхронного кода (в ответ на
  сообщение от вкладки ВСК, а не на прямой клик) не упирается в
  ошибку браузера про «user activation», как упёрлась бы при попытке
  писать обратно в файл, выбранный через диалог «Открыть»;
- если сохранение всё же не удалось (файл недоступен, страница панели
  была перезагружена вручную, право на запись отозвано и т. п.) —
  прогон **сразу и громко останавливается** с понятной ошибкой в
  журнале, а не продолжает «обрабатывать» строки, которые на самом
  деле никуда не пишутся. В этом случае запустите проверку заново —
  будет предложено выбрать файл результата снова.

## Устройство (коротко)

- `shared.js` — общие тайминги (`FORM_WAIT_MS`, `RESULT_WAIT_MS`,
  `ROW_TIMEOUT_MS`), подключается и в content.js, и в панель.
- `manager.html` / `manager.js` — управляющая вкладка: чтение Excel
  (SheetJS, `lib/xlsx.full.min.js`, с разбором стилей и комментариев
  для фильтра по строкам), точечная запись результата (`lib/jszip.min.js`
  + `lib/xlsx-cell-writer.js`), независимое назначение/сброс каждого
  из семи селекторов, цикл прогона со сторожевым таймаутом на строку,
  журнал.
- `content.js` — инжектится на `lka.vsk.ru/*`. Автономно продолжает
  прогон после навигации (состояние — в `chrome.storage.local`, ключ
  `run`), ждёт элементы через `MutationObserver` (защитный таймаут —
  верхняя граница на случай сбоя страницы, а не способ определить
  готовность), плюс режим «укажи элемент мышью» для обучения.
- `background.js` — открывает/фокусирует вкладку панели по клику на
  иконку расширения.

## Известные ограничения

- Учитывает, что «Продолжить» может привести как к обновлению той же
  SPA-страницы, так и к полной навигации на другой URL — оба случая
  покрыты, но конкретную вёрстку `lka.vsk.ru` расширение не знает
  заранее: все селекторы задаются вами через клики, ничего не
  захардкожено.
- Сторожевой таймаут на строку (по умолчанию сумма таймаутов внутри
  content.js плюс запас, около 5 минут) — если реальная проверка на
  сайте штатно занимает дольше, строки будут ошибочно помечаться как
  зависшие; значение задано в `shared.js`.
